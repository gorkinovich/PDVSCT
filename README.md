# PlayDate VS Code Template
This is a [PlayDate](https://play.date/) project template for [Visual Studio Code](https://code.visualstudio.com/) in Windows. You will need to:
* Create the `PLAYDATE_SDK_PATH` variable with the path of the PlayDate SDK.
* Add the bin path of the PlayDate SDK inside the `PATH` variable.
